# Optional server-side product API override.
PRODUCTS_API_URL=https://dummyjson.com/products

# Optional public GraphQL endpoint. Leave empty to use the built-in demo news.
NEXT_PUBLIC_FAKEQL_ENDPOINT=
