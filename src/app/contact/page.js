import ContactForm from "../../components/ContactForm";

export const metadata = {
  title: "Contact | NextStore",
  description: "Contact the NextStore sample project.",
};

export default function ContactPage() {
  return <ContactForm />;
}
